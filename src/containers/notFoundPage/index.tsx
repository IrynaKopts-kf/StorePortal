import React from 'react';

export namespace NotFoundPage {
  export interface Props {
  }
}

export const NotFoundPage = ({ }: NotFoundPage.Props): JSX.Element => {

  return (
    <header>
      <h1>NotFound page</h1>
    </header>
  );
};
